# One file of suites per capability of the plugin, each running the features tagged with it.
imports:
    - suites/carrier_services.yml
    - suites/rate_failure_policy.yml
    - suites/shipment_packaging.yml
    - suites/shipping_origin.yml
    - suites/rate_quoting.yml
